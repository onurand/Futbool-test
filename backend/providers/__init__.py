"""External data providers.

`use_mock_providers=True` (default in dev) routes everything to the mock
implementations so we can develop end-to-end without live API keys. Flip to
False once Sportmonks / The Odds API are live.
"""
